services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: shefa
      POSTGRES_PASSWORD: shefa
      POSTGRES_DB: shefa
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
